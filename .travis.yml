language: java

install: true

before_install:
- nvm install 12
before_script:
- cd frontend
- npm install
- npm install -g @vue/cli
- cd ..
jdk:
- oraclejdk11
script:
- ./gradlew clean test build -Pfrontend